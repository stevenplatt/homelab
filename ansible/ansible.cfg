[defaults]
timeout = 60
transport = local

[connection]
# reduces the number of sudo operations
pipelining = True

[privilege_escalation]
become = True
become_method = sudo
# -n: never prompt — relies on passwordless sudo (sudo-rs prompt
# format breaks ansible's prompt detection until PR #86175 ships)
become_flags = -H -n